name: boardview
description: Custom item and page re-ordering library for Roblu.
version: 0.1.0
homepage: https://github.com/RobluScouting/FlutterBoardView

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  smooth_page_indicator:
    git: https://github.com/Nolence/smooth_page_indicator.git
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
